# 项目补充文档与历史材料

这里存放不参与运行时导入、但需要随项目保留的设计稿和历史操作记录。

## 目录

- `history/commit_messages/`：历史实验对应的提交说明文本，不是当前执行计划。
- `design/taiji-front-redesign/`：前端视觉设计与静态页面草案，不是生产 Vue 前端。

生产代码和可执行入口位于根目录的 `seed/`、`taiji/`、`api/`、`desktop/`、`frontend/`、`scripts/` 和 `tests/`；`neuroplex/` 是冻结对照。
